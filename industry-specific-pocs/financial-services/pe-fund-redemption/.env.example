# Bedrock Agent Core Configuration
# Set this to your full agent runtime ARN after deploying your agent
AGENTCORE_RUNTIME_ARN=arn:aws:bedrock-agentcore:us-east-1:123456789012:runtime/strands_agent_xyz

# Notification Configuration
# Email address to receive user signup notifications
NOTIFICATION_EMAIL=your-email@example.com

# AWS Configuration (optional - will use default profile if not specified)
# AWS_REGION=us-east-1
# AWS_PROFILE=default
