# WebSocket Configuration
# For local development, specify the backend WebSocket URL
# For production (CloudFront deployment), leave this unset to use relative paths
# VITE_WS_URL=ws://localhost:8080/ws
