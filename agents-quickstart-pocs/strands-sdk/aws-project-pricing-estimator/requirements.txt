# Python dependencies for AWS pricing project
# UV will automatically resolve and install additional dependencies

# Core dependencies
strands-agents>=1.4.0
mcp>=1.13.0
streamlit>=1.48.0

# AWS SDK
boto3>=1.40.0
botocore>=1.40.0

# HTTP and async support
aiohttp>=3.12.0
asyncio-mqtt>=0.16.0
requests>=2.32.0

# Note: UV will automatically install many additional packages including:
# - pandas, numpy, altair (for Streamlit charts)
# - pydantic, protobuf (for MCP protocol)
# - jinja2, markupsafe (for Streamlit templates)
# - and many other dependencies automatically resolved by UV

